# Django web framework
Django>=4.2,<5.0
djangorestframework
gunicorn
whitenoise

# Environment & config
python-dotenv

# Satellite & image processing
requests
pillow
numpy
opencv-python-headless

# AI / Gemini
google-generativeai

# OAuth (Copernicus)
oauthlib
requests-oauthlib

# Optional: heavy ML (torch/transformers too large for Vercel ~50MB limit)
# Install locally or use Railway/Render for full ML support
# torch
# transformers
# sentinelhub